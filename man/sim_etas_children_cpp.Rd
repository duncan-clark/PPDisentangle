% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{sim_etas_children_cpp}
\alias{sim_etas_children_cpp}
\title{Simulate ETAS offspring via BFS branching}
\usage{
sim_etas_children_cpp(
  parent_x,
  parent_y,
  parent_t,
  parent_mag,
  A,
  alpha_m,
  cc,
  p,
  D,
  gamma_par,
  q,
  m0,
  beta_gr,
  t_min,
  t_max,
  x_min,
  x_max,
  y_min,
  y_max,
  t_trunc = -1,
  mag_pool = as.numeric(c())
)
}
\arguments{
\item{parent_x, parent_y, parent_t, parent_mag}{Numeric vectors of parent
coordinates and magnitudes.}

\item{A}{Productivity scaling.}

\item{alpha_m}{Magnitude efficiency.}

\item{cc}{Omori-Utsu temporal offset (\eqn{c > 0}).}

\item{p}{Omori-Utsu temporal exponent (\eqn{p > 1}).}

\item{D}{Spatial spread base parameter.}

\item{gamma_par}{Magnitude-spatial scaling (\eqn{\gamma}).}

\item{q}{Spatial power-law exponent (\eqn{q > 1}).}

\item{m0}{Reference magnitude.}

\item{beta_gr}{Gutenberg-Richter \eqn{\beta = b \ln 10}.  Set to 0 or
negative to use empirical resampling from \code{mag_pool}.}

\item{t_min, t_max}{Temporal observation window.}

\item{x_min, x_max, y_min, y_max}{Spatial bounding box.}

\item{t_trunc}{Temporal truncation (negative to disable).}

\item{mag_pool}{Numeric vector of observed magnitudes for resampling.
Ignored when \code{beta_gr > 0}.}
}
\value{
A \code{DataFrame} with columns \code{x}, \code{y}, \code{t},
\code{mag} containing all offspring (excluding parents).
}
\description{
Starting from a set of parent events, generates all offspring (and their
descendants) using the ETAS triggering kernel with Omori-Utsu temporal
delays and isotropic power-law spatial displacements.
}
\details{
\strong{Temporal delays} are sampled by inverse-CDF from the Omori-Utsu
distribution:
\deqn{\Delta t = c\bigl[(1 - u)^{-1/(p-1)} - 1\bigr], \quad u \sim
  \mathrm{Uniform}(0, G_{\max})}
where \eqn{G_{\max} = 1} (no truncation) or
\eqn{G_{\max} = 1 - (1 + t_{\mathrm{trunc}}/c)^{-(p-1)}} (truncated).

\strong{Spatial displacements} are sampled by inverse-CDF from the isotropic
power-law kernel (magnitude-dependent spread):
\deqn{r^2 = d(m)\bigl[(1 - u)^{-1/(q-1)} - 1\bigr], \quad
  d(m) = D\exp\bigl(\gamma(m - m_0)\bigr)}
with direction uniform on \eqn{[0, 2\pi)}.

\strong{Offspring magnitudes} are drawn either from a Gutenberg-Richter
distribution \eqn{m = m_0 + \mathrm{Exp}(\beta_{GR})} (when
\code{beta_gr > 0}) or by resampling uniformly from the supplied
\code{mag_pool} vector.
}
