% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etas.R
\name{generate_inhomogeneous_etas}
\alias{generate_inhomogeneous_etas}
\title{Generate inhomogeneous ETAS with different parameters per region}
\usage{
generate_inhomogeneous_etas(
  Omega,
  partition,
  time_window,
  partition_processes,
  etas_params,
  m0,
  beta_gr = NULL,
  mag_pool = NULL,
  state_spaces = NULL,
  filtration = NULL,
  t_trunc = NULL,
  ...
)
}
\arguments{
\item{Omega}{Full state space (\code{owin}).}

\item{partition}{A \code{spatstat::tess} object.}

\item{time_window}{Numeric vector \code{c(start, end)}.}

\item{partition_processes}{Character vector of process names per tile.}

\item{etas_params}{Named list of ETAS parameter lists
(e.g.\ \code{list(control = ..., treated = ...)}).}

\item{m0}{Reference magnitude.}

\item{beta_gr}{Gutenberg-Richter \eqn{\beta} (or \code{NULL} for
resampling from \code{mag_pool}).}

\item{mag_pool}{Numeric vector for magnitude resampling.}

\item{state_spaces}{Optional pre-computed list of per-process \code{owin}.}

\item{filtration}{Optional data frame of history points.}

\item{t_trunc}{Temporal truncation.}

\item{...}{Additional arguments (e.g.\ \code{covariate_lookup}).}
}
\value{
A \code{data.table} with columns
\code{x, y, t, mag, n, background, W, process, location_process,
  tile_index}.
}
\description{
Multi-region ETAS analogue of \code{generate_inhomogeneous_hawkes}.
Each tile in the partition can have its own ETAS parameter set.
Offspring can spill across tile boundaries.
}
