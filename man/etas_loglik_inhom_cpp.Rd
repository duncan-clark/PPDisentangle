% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{etas_loglik_inhom_cpp}
\alias{etas_loglik_inhom_cpp}
\title{ETAS log-likelihood for an inhomogeneous spatio-temporal point process}
\usage{
etas_loglik_inhom_cpp(
  t,
  x,
  y,
  mag,
  W_val,
  mu,
  A,
  alpha_m,
  cc,
  p,
  D,
  gamma_par,
  q,
  m0,
  areaS,
  t_max,
  t_trunc = -1
)
}
\arguments{
\item{t}{Numeric vector of event times (sorted ascending, shifted
so the window starts at 0).}

\item{x}{Numeric vector of x-coordinates.}

\item{y}{Numeric vector of y-coordinates.}

\item{mag}{Numeric vector of event magnitudes.}

\item{W_val}{Numeric vector of inhomogeneous background weights (set to
1 for homogeneous, 0 for points in a zero-background region).}

\item{mu}{Background rate (events per unit time over the full window).}

\item{A}{Productivity scaling constant.}

\item{alpha_m}{Magnitude efficiency parameter.}

\item{cc}{Omori-Utsu temporal offset (\eqn{c > 0}).  Named \code{cc}
to avoid collision with R's \code{c()} function.}

\item{p}{Omori-Utsu temporal exponent (\eqn{p > 1}).}

\item{D}{Spatial spread base parameter (\eqn{D > 0}).}

\item{gamma_par}{Magnitude-dependent spatial scaling (\eqn{\gamma \ge 0}).}

\item{q}{Spatial power-law exponent (\eqn{q > 1}).}

\item{m0}{Reference (cutoff) magnitude.}

\item{areaS}{Area of the spatial observation window \eqn{|S|}.}

\item{t_max}{Length of the temporal observation window \eqn{T}.}

\item{t_trunc}{Temporal truncation horizon.  Set to a negative value
(default \code{-1}) to disable truncation.}
}
\value{
Scalar log-likelihood value.
}
\description{
Computes the log-likelihood of an Epidemic-Type Aftershock Sequence (ETAS)
model on a marked spatio-temporal point pattern.
}
\details{
\strong{Conditional intensity:}
\deqn{\lambda(t,x,y) = \frac{\mu}{|S|} W(x,y) +
  \sum_{t_j < t} \kappa(m_j)\, g(t - t_j)\, f(x - x_j, y - y_j \mid m_j)}

\strong{Productivity:}
\deqn{\kappa(m) = A \exp\bigl(\alpha_m (m - m_0)\bigr)}

\strong{Omori-Utsu temporal kernel (normalised density on \eqn{[0,\infty)}
when \eqn{p > 1}):}
\deqn{g(\Delta t) = \frac{p - 1}{c}\Bigl(1 + \frac{\Delta t}{c}\Bigr)^{-p}}

\strong{Isotropic power-law spatial kernel (Zhuang et al. 2002):}
\deqn{f(x,y \mid m) = \frac{q - 1}{\pi\, d(m)}
  \Bigl(1 + \frac{r^2}{d(m)}\Bigr)^{-q}, \quad r^2 = x^2 + y^2}
with \eqn{d(m) = D \exp\bigl(\gamma (m - m_0)\bigr)}.

\strong{Compensator (integral of intensity):}
\deqn{\int_0^T \lambda\,\mathrm{d}t \approx \mu T +
  \sum_i \kappa(m_i)\bigl[1 - \bigl(1 + h_i / c\bigr)^{-(p-1)}\bigr]}
where \eqn{h_i = \min(T - t_i,\; t_{\mathrm{trunc}})} when temporal
truncation is active and \eqn{h_i = T - t_i} otherwise.  The spatial
kernel integrates to 1 over \eqn{\mathbb{R}^2} (infinite-plane
approximation, consistent with the Hawkes implementation).

When truncation is active the temporal density is renormalised so that
it integrates to 1 over \eqn{[0, t_{\mathrm{trunc}}]}:
\deqn{G(t_{\mathrm{trunc}}) = 1 - (1 + t_{\mathrm{trunc}}/c)^{-(p-1)}}
}
