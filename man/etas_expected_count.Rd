% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ate.R
\name{etas_expected_count}
\alias{etas_expected_count}
\title{Analytic expected count for an ETAS process}
\usage{
etas_expected_count(params, windowT, beta_gr, m0 = NULL)
}
\arguments{
\item{params}{Named list with at least mu, A, alpha_m.}

\item{windowT}{Numeric c(start, end).}

\item{beta_gr}{Gutenberg-Richter beta.}

\item{m0}{Reference magnitude (unused in the formula but kept for API).}
}
\value{
List with E_N (expected count, NA if supercritical), E_N_bg
(background-only count), and eta (branching ratio).
}
\description{
For a univariate ETAS with Gutenberg-Richter magnitudes (exponential
rate beta_gr), the expected total count on the full spatial domain
over a time window of length T is  mu * T / (1 - eta),
where eta = A * beta_gr / (beta_gr - alpha_m) is the branching ratio.
}
\details{
When eta >= 1 (supercritical) the infinite-time expectation diverges.
In this case we report both the background-only count (mu * T) and
mark the total as NA (must use simulation).
}
