% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{etas_bivariate_loglik_cpp}
\alias{etas_bivariate_loglik_cpp}
\title{Bivariate ETAS log-likelihood with cross-excitation}
\usage{
etas_bivariate_loglik_cpp(
  t,
  x,
  y,
  mag,
  process_id,
  W_val_0,
  W_val_1,
  mu_0,
  mu_1,
  A_00,
  alpha_m_00,
  A_11,
  alpha_m_11,
  A_01,
  alpha_m_01,
  A_10,
  alpha_m_10,
  cc,
  p,
  D,
  gamma_par,
  q,
  m0,
  areaS_0,
  areaS_1,
  t_max,
  t_trunc = -1
)
}
\arguments{
\item{t}{Event times (sorted ascending, window starts at 0).}

\item{x, y}{Spatial coordinates.}

\item{mag}{Event magnitudes.}

\item{process_id}{Integer vector: 0 = control, 1 = treated.}

\item{W_val_0}{Background weights for control process (0 in treated region).}

\item{W_val_1}{Background weights for treated process (0 in control region).}

\item{mu_0, mu_1}{Background rates for control and treated.}

\item{A_00, alpha_m_00}{Control self-excitation productivity.}

\item{A_11, alpha_m_11}{Treated self-excitation productivity.}

\item{A_01, alpha_m_01}{Treated-to-control cross-excitation.}

\item{A_10, alpha_m_10}{Control-to-treated cross-excitation.}

\item{cc, p, D, gamma_par, q}{Shared structural parameters.}

\item{m0}{Reference magnitude.}

\item{areaS_0}{Active area for control background.}

\item{areaS_1}{Active area for treated background.}

\item{t_max}{Length of temporal observation window.}

\item{t_trunc}{Temporal truncation (negative to disable).}
}
\value{
Scalar joint log-likelihood.
}
\description{
Computes the joint log-likelihood of a two-component ETAS model where
events in each process can trigger offspring in both processes via a
2x2 kernel matrix.
}
\details{
The conditional intensity for process k in {0, 1} is:
\deqn{\lambda_k(t,x,y) = \frac{\mu_k}{|S_k|} W_k(x,y) +
  \sum_{l \in \{0,1\}} \sum_{t_j < t,\, \mathrm{proc}_j = l}
    A_{kl}\,e^{\alpha_{kl}(m_j - m_0)}\,g(\Delta t)\,f(\Delta x, \Delta y | m_j)}

The four kernel components (child, parent) are:
\itemize{
\item (0,0): control self-excitation
\item (0,1): treated events exciting control intensity
\item (1,0): control events exciting treated intensity
\item (1,1): treated self-excitation
}

Temporal and spatial kernels g, f use shared structural parameters
(c, p, D, gamma, q), identical to the univariate ETAS formulation.
}
