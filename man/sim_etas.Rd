% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etas.R
\name{sim_etas}
\alias{sim_etas}
\alias{sim_etas_fast}
\title{Simulate a spatio-temporal ETAS process}
\usage{
sim_etas(
  params,
  windowT,
  windowS,
  m0,
  beta_gr = NULL,
  mag_pool = NULL,
  background_realization = NULL,
  filtration = NULL,
  covariate_lookup = NULL,
  t_trunc = NULL,
  ...
)

sim_etas_fast(
  params,
  windowT,
  windowS,
  m0,
  beta_gr = NULL,
  mag_pool = NULL,
  background_realization = NULL,
  filtration = NULL,
  covariate_lookup = NULL,
  t_trunc = NULL,
  ...
)
}
\arguments{
\item{params}{Named list with elements
\code{mu, A, alpha_m, c, p, D, gamma, q}.}

\item{windowT}{Numeric vector \code{c(start, end)}.}

\item{windowS}{An \code{owin} object or numeric vector
\code{c(xmin, xmax, ymin, ymax)}.}

\item{m0}{Reference magnitude.}

\item{beta_gr}{Gutenberg-Richter \eqn{\beta = b \ln 10}.  Set to
\code{NULL} or \eqn{\le 0} to use empirical resampling.}

\item{mag_pool}{Numeric vector of magnitudes for resampling.}

\item{background_realization}{Optional list with \code{x, y, t, mag}
to use as predetermined background events.}

\item{filtration}{Optional data frame of history points that can trigger
offspring but are not themselves part of the new realisation.}

\item{covariate_lookup}{Function(x, y) returning spatial covariate
values, or \code{NULL} for homogeneous background.}

\item{t_trunc}{Temporal truncation (\code{NULL} = none).}

\item{...}{Additional arguments (ignored).}
}
\value{
A list with elements \code{x, y, t, mag, n, background, W}.
}
\description{
Generates a realisation of the ETAS model via Poisson-cluster (branching)
simulation.  Background events are thinned from a homogeneous (or
inhomogeneous) Poisson process; offspring are generated in C++ via
\code{sim_etas_children_cpp}.
}
\section{Magnitude assignment}{

Offspring magnitudes are drawn from a Gutenberg-Richter distribution
\eqn{m = m_0 + \mathrm{Exp}(\beta_{GR})} when \code{beta_gr > 0},
or resampled from \code{mag_pool} otherwise.  Background magnitudes
use the same mechanism.
}

