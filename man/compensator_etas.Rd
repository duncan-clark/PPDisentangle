% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/etas.R
\name{compensator_etas}
\alias{compensator_etas}
\title{Compensator of spatio-temporal ETAS process}
\usage{
compensator_etas(
  params,
  realiz,
  windowT,
  windowS,
  m0 = NULL,
  zero_background_region = NULL,
  ...
)
}
\arguments{
\item{params}{Named numeric vector or list with elements
\code{mu, A, alpha_m, c, p, D, gamma, q}.}

\item{realiz}{Data frame with columns \code{x, y, t, mag}
(and optionally a background-rate column, default \code{"W"}).}

\item{windowT}{Numeric vector \code{c(start, end)} for the time window.}

\item{windowS}{An \code{owin} object (or convertible) for the spatial
observation window.}

\item{m0}{Reference magnitude.}

\item{zero_background_region}{Optional \code{owin} where the background
intensity is zero (events may still be triggered there).}

\item{...}{Additional arguments (ignored).}
}
\value{
Numeric vector of compensator values at each event time.
}
\description{
Computes the cumulative compensator \eqn{\Lambda(t_i) =
\int_0^{t_i}\!\int_S \lambda(s,u)\,\mathrm{d}u\,\mathrm{d}s}
evaluated at each event time, for use in residual analysis and
goodness-of-fit testing (random time-change theorem).
}
